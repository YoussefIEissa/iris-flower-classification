# Core libraries required to run the Iris KNN project.
# Install everything with:  pip install -r requirements.txt
#
# Version floors are conservative; newer versions work too.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3

# Optional: only needed to open and run notebooks/iris_knn.ipynb
jupyter>=1.0
